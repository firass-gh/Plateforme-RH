spring.application.name=plateforme-rh
server.port=8081

# Use environment variables for sensitive data
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/PlateformeRh?createDatabaseIfNotExist=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Logging
logging.level.root=info
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Config - This will be injected from an environment variable
jwt.secret=${JWT_SECRET}

# Configuration pour le service d'email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rhappmanagement@gmail.com
spring.mail.password=tmhz yenp lnwr qlvy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true